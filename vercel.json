{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "buildCommand": "gem install jekyll bundler && jekyll build"
}
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
